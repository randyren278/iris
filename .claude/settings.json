{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "cd \"$CLAUDE_PROJECT_DIR\" && if [ -f .checkpoints/ARMED ]; then python3 scripts/checkpoint_runner.py gate; else exit 0; fi" }
        ]
      }
    ]
  }
}
